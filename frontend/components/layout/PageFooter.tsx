export function PageFooter() {
  return (
    <footer className="border-t border-[var(--border-subtle)] py-6 text-center text-[11px] text-[var(--text-muted)]">
      Research aid only — verify figures in original SEC filings. Not investment advice.
    </footer>
  );
}
