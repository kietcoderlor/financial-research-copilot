NEXT_PUBLIC_API_URL=http://localhost:8000
API_BASE_URL=http://localhost:8000

# Must match backend JWT_SECRET for middleware session checks
AUTH_JWT_SECRET=dev-change-me-in-production-use-long-random-string

# Google OAuth (Web client) — same client ID as backend GOOGLE_CLIENT_ID
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
